# still not working

# zephyr_library()  # This registers the library in Zephyr's build system
# zephyr_library_sources(door_monitoring.c)  # Add the source file
# zephyr_library_include_directories(${CMAKE_CURRENT_SOURCE_DIR}/../../include/application)
